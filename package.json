{
  "name": "orbit-switch",
  "version": "1.0.0",
  "private": true,
  "description": "ORBIT SWITCH - one-tap addictive browser game",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "e2e": "node tests/e2e.js",
    "lint": "eslint js tests tools sw.js eslint.config.js",
    "start": "npx http-server -c-1 -p 8080 .",
    "gen:ios": "node tools/gen-ios-strings.js"
  },
  "devDependencies": {
    "eslint": "^10.1.0",
    "playwright": "^1.56.1"
  }
}
