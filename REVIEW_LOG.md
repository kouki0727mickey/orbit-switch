# レビュー & 修正ログ

各ラウンドで「レビュー（コード精読・スクリーンショット確認・自動プレイ検証）→ 問題の特定 → 修正 → テスト再実行」を行った記録です。

| # | 観点 | 発見した問題 | 修正内容 |
|---|------|-------------|---------|
| 1 | アクセシビリティ / E2E | 常時パルスするボタンのせいで E2E のクリックが「要素が安定しない」で失敗。動きに敏感なユーザーにも負担 | `prefers-reduced-motion` でアニメーション停止。E2E は reduced motion で実行 |
| 2 | UX（ゲームオーバー） | 初回プレイ時、スコア=ベストなのに「あと1点でベスト更新！」と表示される | 初記録／記録更新／惜しい／通常の4パターンに文言を分岐 |
| 3 | 描画 | 「CLOSE!」等のポップアップ文字が画面端で見切れる（390px 幅で確認） | テキスト幅を測って画面内にクランプ |
| 4 | UI（メニュー） | キャッチコピーが「ろ。」だけ改行される泣き別れ | `<wbr>` + `word-break: keep-all` + `text-wrap: balance` |
| 5 | ロジック重複 | ジェム獲得ポップアップの点数を UI 側で再計算しており、フィーバー倍率を二重に掛けて誤表示 | 点数はコアで1回だけ計算しイベントに `points` として載せる |
| 6 | 入力 | キーを押しっぱなしにするとキーリピートで高速に切替連打される。フォーカス中のボタン上で Space/Enter を押すと keydown と click の両方で `startGame` が二重実行される | `e.repeat` を無視、メニューではフォーカス中ボタンに処理を任せる、開始時に `blur()` |
| 7 | UX（中断） | タブ切替・通知などで離脱すると、戻った瞬間にゲームが再開して即死する | `visibilitychange`/`blur` で自動一時停止、タップ/Space/Esc で再開、Esc/P で手動ポーズ |
| 8 | 堅牢性 | `window.localStorage` の参照自体が例外を投げる環境（サンドボックス iframe・Cookie ブロック）で起動時にクラッシュ | 起動時に安全にプローブし、失敗したら保存なしで動作 |
| 9 | 堅牢性（セーブ） | 古い/壊れた/改ざんされたセーブ（`owned` が配列でない、数値が NaN、存在しないスキン等）で例外や不正状態 | `sanitize()` で全フィールドを型検証・修復。テスト追加 |
| 10 | セキュリティ | ミッション文言を `innerHTML` にそのまま挿入（セーブ改ざんで self-XSS） | HTML エスケープ + ミッション文言はセーブからではなく定義から再生成 |
| 11 | レイアウト | 横向きスマホ（844×390）でスコア表示が外周リングに重なる | HUD 領域を確保した上でリングが収まるよう `unit` と中心座標 `CX/CY` を算出 |
| 12 | レイアウト | 背の低い画面でゲームオーバー画面の上端が切れ、スクロールしても見えない（flex center のはみ出し問題） | `justify-content: safe center`、safe-area 対応、低い画面用のコンパクト CSS、ボタンを横並びに |
| 13 | 中毒性（摩擦削減） | リトライにはボタンを正確に押す必要がある。一方で死亡直後の連打で結果画面が一瞬で飛ばされる | メニュー/結果画面の空白タップで即スタート。結果画面は 0.6 秒だけ入力ロック。0点時の文言も修正 |
| 14 | ゲームバランス | 速度がスコア連動のため、ジェムや倍率で稼ぐほど急に速くなり「上手いほど理不尽」になる | 速度・難易度を「通過したトゲの数」に連動。テスト追加 |
| 15 | テスト | 公平性テストが反応時間ゼロの完璧なボットのみで、人間にとって理不尽な配置を検出できない | 反応 200ms・タイミング誤差 ±60ms の「人間ボット」を追加し、序盤 30 秒の生存率 95% 以上をテスト |
| 16 | オンボーディング | 初見プレイヤーは「いつタップすべきか」が分からず数秒で死んで離脱しがち | 最初の3プレイ限定で、自分のリングにトゲが迫り反対側が安全な時に「TAP!」を表示。開始直後は中央に操作説明 |
| 17 | 中毒性（目標の可視化） | フィーバーまであと何個か見えず、コンボ継続の動機が弱い | HUD にコンボ数とフィーバーゲージ（発動中は残り時間）を追加 |
| 18 | 中毒性（メタ） | 結果画面で「次の目標」が見えず、もう1回の理由が弱い | 次に解放できるスキンまでの残りコインを表示（買える場合は「解放できます！」） |
| 19 | 手触り（ジュース） | 死亡の瞬間が軽く、ニアミスも気持ちよさが薄い | 死亡時 0.12 秒のヒットストップ、ニアミス時 0.12 秒のスローモーション |
| 20 | パフォーマンス / 手触り | HUD の DOM を毎フレーム書き換えていた。加点の手応えもない | 値が変わった時だけ更新し、加点時にスコアが弾むアニメーション |
| 21 | 中毒性 / 設計 | メニューが静止画で「遊んでみたい」気持ちを誘わない。自動操縦ロジックがテスト側にしかなくブラウザで検証できない | オートパイロットをコアへ移し、メニュー背後で半透明のデモプレイ（アトラクトモード）を常時再生。`?autoplay` テストフックも追加 |
| 22 | パフォーマンス | トゲ・ジェム1個ごとに `shadowBlur` 付きで描画（モバイルで非常に重い） | 発光図形をサイズ/色ごとにオフスクリーンへ事前描画し `drawImage` で描画（キャッシュ上限付き） |
| 23 | パフォーマンス | 背景のラジアルグラデーションを毎フレーム生成 | 画面サイズ・フィーバー状態をキーにキャッシュ |
| 24 | パフォーマンス | コアのサブステップ（最大 120回/秒）ごとに `filter` で新しい配列を生成 | インプレースで詰め直してGC圧を削減 |
| 25 | 描画 / 一貫性 | 軌跡がフレーム数基準で、120Hz 端末では短く、高速時は点が離れて見える | ゲーム内時間 0.2 秒分を先細りのリボンとして描画。E2E にオートプレイ・ポーズ/再開・フレーム時間計測を追加 |
| 26 | 公平性 | フィーバー終了の瞬間にトゲと重なっていると即死する（直前まで無敵だったのに理不尽） | フィーバー終了後 0.6 秒の無敵猶予 `grace`。テスト追加 |
| 27 | 可読性 | 当たり判定に `1.6` などのマジックナンバーと読みにくい三項演算子の連鎖 | `gemMagnet`・`passBehind` を CONFIG 化し、判定式を分解 |
| 28 | ゲームバランス（バグ発見） | 終盤に変化がなく人間ボットでも延々生き残る → ジグザグ配置を追加したところ、完璧ボットが毎回 40 秒前後で死亡。原因は**パターンが 1 周（2π）以上先に配置され、現在位置と物理的に重なる「見えない壁」** | パターンは `pending` キューに入れ、スポーン地平線内に入った時だけ実体化。「地平線より先に実体がない」不変条件テストを追加。完璧ボットは 300 秒全シード生存、雑なタイミングは終盤で咎められるように |
| 29 | サウンド / フィードバック | ジェム音の音程がコンボ 11 以降ずっと同じで、フィーバーとの関係も無い | 音程がフィーバーまでの進捗に合わせて上昇→リセット（耳でフィーバーの近さが分かる） |
| 30 | サウンド | アルペジオを `setTimeout` で鳴らしておりタイミングがブレる／タブ非表示で遅延 | すべて AudioContext の時間軸で予約再生 |
| 31 | 中毒性（拡散） | スコアを友達に見せる手段がない | 結果画面に共有ボタン（Web Share API、非対応ならクリップボードにコピーしてトースト表示） |
| 32 | 定着率（PWA） | ホーム画面に追加できず、アイコン・説明文もない | Web App Manifest・SVG アイコン・meta description を追加 |
| 33 | 手触り | スマホで振動フィードバックがない | 死亡・フィーバー・ニアミス・記録更新で `navigator.vibrate`（ミュート時は無効、例外は握りつぶす） |
| 34 | アクセシビリティ | キーボード操作時のフォーカス表示がなく、canvas に説明もない | `:focus-visible` のアウトライン、canvas の aria-label、結果スコアを `aria-live` に。reduced-motion の対象も拡大 |
| 35 | 報酬演出 | 獲得コインやミッション達成が数字で出るだけで達成感が薄い | コインのカウントアップ、達成ミッションのポップ演出、レベルアップ時も効果音 |
| 36 | テスト | E2E が `file://` で実行されており、実運用（HTTP 配信）での 404・マニフェスト・SW の問題を検出できない | テスト内に依存なしの静的 HTTP サーバーを立て、HTTP 4xx/5xx もエラーとして検出 |
| 37 | 定着率（オフライン） | 電車内など圏外で遊べない | Service Worker（stale-while-revalidate）を追加。E2E で「一度訪問→オフラインで再読込→プレイ開始」を検証 |
| 38 | 保守性 | 依存がグローバルインストール頼みで CI もない | `playwright`/`eslint` を devDependencies に固定、GitHub Actions で lint・unit・E2E を実行しスクショを成果物に |
| 39 | ドキュメント | README がタイトルのみ | 遊び方・中毒性の仕掛け・起動/開発方法・構成・公平性の担保を記載 |
| 40 | UX（中断復帰） | 一時停止画面は小さなボタンでしか再開できず、再開した瞬間に時間が動き出すので直後に死にやすい | 画面のどこをタップしても再開、再開後 0.8 秒の「READY」停止（その間の入力は無視） |
| 41 | メタ | デイリーボーナスがページ読込時にしか判定されず、PWA を開きっぱなしだと日付をまたいでも貰えない | タブ復帰時（`visibilitychange`）にも判定 |
| 42 | UX（ショップ） | コイン不足のスキンを押しても無反応で、壊れているように見える | ボタンを揺らし「あと 🪙N」を表示。`aria-pressed` で選択状態を伝達 |
| 43 | アクセシビリティ | 画面揺れ・白フラッシュが `prefers-reduced-motion` を無視していた | reduced motion 時は canvas の揺れ・フラッシュを無効化 |
| 44 | 公平性 / フィードバック | フィーバーがいつ終わるか分からず、終了直後の無敵猶予も見えない | 残り 1 秒で自機が点滅、終了時に「FEVER END」、無敵猶予中も点滅 |
| 45 | 中毒性（プレイ中の緊張感） | プレイ中に自己ベストが見えず「あと少しで更新！」という最大のフックが働かない | HUD にベストを表示し、超えた瞬間に「NEW BEST!」ポップ＋効果音＋振動 |
| 46 | 経済バランス（重大） | シミュレーションしたところ、コインがスコアに比例するため（スコアは倍率で超線形に伸びる）初心者でも1回目でスキン購入、上級者は数回で全スキン解放 → 長期目標が崩壊 | コインを √スコア ベースの逓減式に、レベル曲線を急に、レベル/ミッション報酬と価格を再調整し、スキンを1つ追加。「初心者は3〜8回で最初のスキン」「1回の神プレイで店を買い占められない」をテストで固定 |
| 47 | 報酬のタイミング | ミッション達成が死後の結果画面まで分からない（報酬が遅れると快感が薄れる） | プレイ中に達成した瞬間「✅ ミッション名」をポップ表示 |
| 48 | 中毒性（毎日の来訪理由） | デイリーボーナス以外に「今日やる理由」がなく、友達と同条件で競う手段もない | 日付のハッシュをシードにした「今日のチャレンジ」（全員同じステージ・当日ベストと挑戦回数を別管理・翌日リセット・専用の共有文言）。ユニット/E2E テスト追加 |
| 49 | コード品質 | 未使用コード（`Sfx.isMuted`、パーティクルの `max`、トゲ通過ごとに生成される未使用の `pass` イベント、読まれない `switches`）。`smashed`/`totalGems` は集計しているのに表示していない | 未使用コードを削除。粉砕数を結果画面に、通算プレイ数・ジェム・レベルをミッション画面に表示 |
| 50 | 正確性（全体再レビュー） | 日付をまたいだデイリーチャレンジのプレイが「翌日（別ステージ）」の記録として保存される。HUD が毎フレーム日付文字列を生成 | 開始時に日付とベストを確定させて保持 |
| 51 | 中毒性（ショップ導線） | 買えるスキンと買えないスキンの見分けがつかず、メニューからも購入可能なことが分からない | 購入可能なスキンを金枠で強調し「購入」表示。購入可能な時はメニューの「スキン」ボタンに赤いバッジ |
| 52 | テスト | ショップの購入フロー（バッジ・不足時フィードバック・購入・装備・残高）が E2E で未検証 | セーブにコインを付与してバッジ→不足時メッセージ→購入→装備・残高を検証する E2E を追加 |

## 最終状態

- ユニットテスト 25 件（決定性・公平性ボット・「見えない壁」の不変条件・経済バランス・セーブ修復・デイリー）すべて成功
- E2E（HTTP 配信・スマホ縦/PC・オートプレイ・ポーズ・デイリー・ショップ購入・オフライン）すべて成功、コンソールエラー/HTTP エラーなし
- ESLint エラーなし
